--!strict
-- init.lua
-- Entry point so the repo can be required as a single module, e.g.
--   local Library = require(ReplicatedStorage.MyUILib)
-- or, if using Wally: local Library = require(Packages.MyUILib)

return require(script.Library)
